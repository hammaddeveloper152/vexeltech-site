import React from 'react';
import { Link } from 'react-router-dom';
export function Section({label,title,children,dark=false,concrete=false}){return <section className={`band ${dark?'band--ink ':''}${concrete?'band--concrete ':''}`}><div className="wrap"><span className="label">{label}</span><h2 className="h-lg">{title}<span className="dot">.</span></h2>{children}</div></section>}
export function Cards({items}){return <div className="ggrid">{items.map((x,i)=>{const content=<><div className="gcard-top">{x.image?<img className="gcard-image" src={x.image} alt=""/>:<span aria-hidden="true" className="gcard-icon">{x.icon||String(i+1).padStart(2,'0')}</span>}<div className="gcard-n">{x.title}</div></div><div className="gcard-r"><b>{x.kicker||'Included'}</b></div><div className="gcard-m">{x.text}</div>{x.linkLabel&&<span className="gcard-link">{x.linkLabel} <span aria-hidden="true">-&gt;</span></span>}</>;return x.href?<Link className="gcard" key={i} to={x.href}>{content}</Link>:<article className="gcard" key={i}>{content}</article>})}</div>}
export function CTA({title='Ready to start?',text='Tell us what you need and we will tell you what we would fix first.',buttonText='Get a Custom Quote'}){return <section className="band band--ink"><div className="wrap"><span className="label">Next</span><h2 className="h-lg">{title}<span className="dot">.</span></h2><p className="lede">{text}</p><div className="btns"><Link className="btn btn--go" to="/contact-us">{buttonText}</Link><Link className="btn btn--line" to="/contact-us">Book a Discovery Call</Link></div></div></section>}
