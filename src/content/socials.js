/* THE SOCIAL ACCOUNTS, 2026-09-24 (the founder's energy pass). The one place
   their URLs are set. EMPTY UNTIL THE FOUNDER SUPPLIES THEM: an account with
   no URL does not render, and with none set the footer's socials row is not
   rendered at all, so the band closes up with no gap where it was. Fill a
   URL here and its circle appears. When they land, add them to index.html's
   JSON-LD `sameAs` in the same pass. */
export const SOCIAL_URLS = {
  linkedin: '',
  instagram: '',
  facebook: '',
};
